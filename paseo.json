{
  "worktree": {
    "setup": [
      "cp \"$PASEO_WORKTREE_PATH/.paseo/paseo.user.bazelrc\" \"$PASEO_WORKTREE_PATH/user.bazelrc\""
    ],
    "teardown": [
      "bash \"$PASEO_WORKTREE_PATH/.paseo/reclaim-bazel-output-base.sh\"",
      "if git rev-parse --verify --quiet \"refs/heads/$PASEO_BRANCH_NAME\" >/dev/null 2>&1; then git checkout --detach && git branch -D \"$PASEO_BRANCH_NAME\"; fi"
    ]
  }
}
